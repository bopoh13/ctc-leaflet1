deploy:
  provider: pages
  skip-cleanup: true
  github-token: LYYlVMbAyobShMTBQbhJwg  # Set in the settings page of your repository, as a secure variable
  keep-history: true
  on:
    branch: master

language: ruby
rvm:
- 2.3.3
script: "bundle exec jekyll build"

sudo: false # if error set "required"
